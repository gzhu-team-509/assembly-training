// Unfinished
#include <iostream>
#include <vector>
using namespace std;

struct resturant
{
    int f, t;
    double val;
} cur;

vector<resturant> list;

int main()
{
    int n, k; cin >> n >> k;
    while (n--)
    {
        cin >> cur.f >> cur.t;
        list.push_back(cur);
    }

    whlie (k > 0)
    {

    }
}
